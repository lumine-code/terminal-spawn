{
  "name": "terminal-spawn",
  "author": "lumine-code",
  "description": "Spawn the system terminal in the current file or project directory.",
  "repository": "https://github.com/lumine-code/terminal-spawn",
  "bugs": {
    "url": "https://github.com/lumine-code/terminal-spawn/issues"
  },
  "keywords": [
    "shell",
    "cmd",
    "console",
    "external",
    "launch",
    "os-terminal"
  ],
  "version": "1.0.0",
  "license": "MIT",
  "engines": {
    "lumine": "^1.0.0"
  },
  "main": "./lib/main",
  "files": [
    "docs",
    "keymaps",
    "lib",
    "menus",
    "spec"
  ],
  "scripts": {
    "test": "lumine --test spec",
    "lint": "eslint . --max-warnings 0",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "configSchema": {
    "command": {
      "title": "Terminal command",
      "description": "Shell command used to launch the terminal. Use {cwd} as a placeholder; leave blank for the platform default.",
      "type": "string",
      "default": ""
    },
    "commandWithArgs": {
      "title": "Terminal command with arguments",
      "description": "Template used to launch a terminal command. Use {cwd} and {command} as placeholders; leave blank for the platform default.",
      "type": "string",
      "default": ""
    }
  },
  "providedServices": {
    "terminal-spawn": {
      "description": "Spawn the configured external terminal at a path, optionally with a command",
      "versions": {
        "1.0.0": "provideTerminalSpawn"
      }
    },
    "background-tips.provider": {
      "versions": {
        "1.0.0": "provideBackgroundTips"
      }
    }
  },
  "consumedServices": {
    "mcp.bridge": {
      "versions": {
        "^1.1.0": "consumeMcpBridge"
      }
    }
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.9.1",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-n": "^18.3.0",
    "globals": "^17.11.0",
    "prettier": "^3.9.6"
  }
}
